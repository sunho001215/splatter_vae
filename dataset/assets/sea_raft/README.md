# SEA-RAFT checkpoint

Place the official `Tartan-C-T-TSKH-spring540x960-M.pth` checkpoint here.
The expected SHA-256 is:

```text
adcc169244e99d4e6fe645b60aa8eaf3e4263698a3e870b8fbae618e3d2acc28
```

The checkpoint is intentionally ignored by Git; the SEA-RAFT source itself is
pinned as the `dataset/third_party/SEA-RAFT` submodule.
